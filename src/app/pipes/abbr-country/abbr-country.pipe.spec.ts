import { AbbrCountryPipe } from './abbr-country.pipe';

describe('AbbrCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new AbbrCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
